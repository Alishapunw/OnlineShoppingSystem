import { RetailerChangePassword } from './retailer-change-password';

describe('RetailerChangePassword', () => {
  it('should create an instance', () => {
    expect(new RetailerChangePassword()).toBeTruthy();
  });
});
